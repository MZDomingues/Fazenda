CREATE TABLE FAZENDA (
    ID               INTEGER NOT NULL,
    NOME             VARCHAR(100) NOT NULL
);

ALTER TABLE FAZENDA ADD CONSTRAINT PK_FAZENDA PRIMARY KEY (ID);
ALTER TABLE FAZENDA ADD CONSTRAINT UK_NOME UNIQUE (NOME);