CREATE TABLE ANIMAL (
    ID               INTEGER NOT NULL,
    ID_FAZENDA       INTEGER NOT NULL,
    TAG              NUMERIC(15) NOT NULL
);

ALTER TABLE ANIMAL ADD CONSTRAINT PK_ANIMAL PRIMARY KEY (ID);
ALTER TABLE ANIMAL ADD CONSTRAINT FK_FAZENDA FOREIGN KEY (ID_FAZENDA) REFERENCES FAZENDA (ID);